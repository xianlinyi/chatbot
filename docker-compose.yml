version: '3.8'
services:
  app:
    build: .
    image: local-agent-chatbot:latest
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - HOST=0.0.0.0
      - PORT=3000
    restart: unless-stopped
    volumes: []
